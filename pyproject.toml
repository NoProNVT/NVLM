[project]
name = "kvasir-medical-report"
version = "0.1.0"
description = "Resource-Efficient Medical Report Generation using Large Language Models"
authors = [
    {name = "AI Assistant"}
]
requires-python = ">=3.10"
dependencies = [
    "torch>=1.13.1",
    "torchvision>=0.14.1",
    "transformers",
    "accelerate",
    "pillow",
    "tqdm",
    "nltk",
    "rouge-score",
    "peft",
    "timm==0.9.7",
    "bitsandbytes>=0.49.2",
]

[tool.uv]
package = false
